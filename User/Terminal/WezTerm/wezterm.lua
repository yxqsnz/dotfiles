local wz = require("wezterm")

return {
	font = wz.font("IBM Plex Mono"),
	font_size = 11,
	line_height = 1.4,
	use_fancy_tab_bar = false,
	tab_bar_at_bottom = true,
	hide_tab_bar_if_only_one_tab = true,
	color_scheme = "carbonfox",
}
